const postPhoto = require('./post');
const DisplayAllPhotos = require('./showAll');
const showPhoto = require('./show');
const deletePicture = require('./delete');
const deleteAllPhotos = require('./deleteAll');

module.exports = {
  postPhoto,
  DisplayAllPhotos,
  showPhoto,
  deletePicture,
  deleteAllPhotos,
};
